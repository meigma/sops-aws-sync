[project]
name = "sops-aws-sync-docs"
version = "0.0.0"
description = "Documentation for reconciling committed SOPS documents with AWS Secrets Manager."
requires-python = ">=3.14"
dependencies = [
    "mkdocs-material>=9.7.7",
]

[tool.uv]
package = false
