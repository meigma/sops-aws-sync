# Technical Notes

- Use hexagonal architecture at all times. Keep business logic isolated from CLI, filesystem, network, storage, and other external adapters.
- Prefer functional testing before calling any feature complete. Unit tests are useful, but they do not prove the tool works the way the design intends.
- Take an agile approach to development. Avoid waterfall: underspecify when useful, prototype early, learn from the result, and refine from working behavior.
- Session 001's `.journal/001/DESIGN.md` is the sole V1 product and architecture authority. Its `.journal/001/PLAN.md` is the subordinate five-phase delivery roadmap and must not add or change design requirements.
- Session 002 completed the accepted disposable Phase 1 proof in PR #7, which was closed without merge. It verified exact-commit go-git reads, stable in-process SOPS JSON decryption with MAC enforcement, strict JSON plus JCS canonicalization, deterministic mapping, and a pure `create` plan without an AWS client. Consult `.journal/002/SUMMARY.md` for evidence and findings.
- Session 003 landed Phase 2 in PR #8 at merge commit `f3f995e`, establishing the durable exact-commit Git/SOPS-to-AWS create/update reconciliation baseline. Consult `.journal/003/SUMMARY.md` for its design decisions, review fixes, verification evidence, and live AWS acceptance result.
- The Phase 2 AWS acceptance test passed through whzbox in `us-east-1`, proving create → no-op → external-drift update → no-op against genuine Secrets Manager; use the opt-in `TestAWSSandboxCreateUpdateNoOpAndVerification` test for future live regression checks.
- Session 004 landed Phase 3 in PR #9 at merge commit `f7d202f`: `master` now contains the complete Go CLI lifecycle reconciliation slice, including exact-scope paginated discovery, desired/discovered-union planning, restoration, recovery-window deletion, empty-state authorization, deletion-last ordering, bounded restore follow-up, and direct affected-target verification. Consult `.journal/004/SUMMARY.md` for the exact decisions and evidence.
- The complete CLI AWS acceptance test passed through whzbox in `us-east-1`, proving create → no-op → drift update → scope discovery → scheduled deletion → empty-state convergence → restore → follow-up update → final convergence; use opt-in `TestAWSSandboxCompleteLifecycleAndVerification` for future live lifecycle regression checks and give each run a unique prefix/scope.
- Session 005 landed Phase 4 in PR #10 at merge commit `8e9db4c`: `master` now contains the thin Node 24 TypeScript Action, including total typed inputs, explicit shell-free CLI argv, exact-version release resolution, checksum and immutable GitHub attestation policy, re-verified cache hits, strict redacted report handling, safe outputs, and committed bundle parity. Consult `.journal/005/SUMMARY.md` for the implementation and verification evidence.
- Current `gh attestation verify` requires authentication even for public artifacts. The optional Action `github-token` input defaults to `${{ github.token }}` so public callers omit configuration without running unauthenticated; private producer repositories must override it with a suitably scoped token.
- Session 006 landed Phase 5 hardening in PR #11 at merge commit `b67210d`: `master` now couples the CLI and Action release version, runs race/vulnerability/workflow checks in the complete Moon gate, rehearses supported binaries/checksums/SBOMs and the committed Action bundle on every PR, and contains V1 operator/security/release documentation. Consult `.journal/006/SUMMARY.md` for decisions, evidence, and deferred work.
- AWS sandbox acceptance is a manual functional test, not repository automation. Do not add a reusable or dispatchable privileged acceptance workflow unless the user explicitly revises that decision.
- V1 publication is not complete: no tag or release exists. Release Please requires its GitHub App client ID/private key, Pages must be enabled for GitHub Actions, and the current private-repository plan rejects the desired protection rules with HTTP 403.
- After publication is explicitly authorized, independently verify the immutable release and run the exact-release manual AWS lifecycle test with caller-owned authentication before claiming complete V1 acceptance.
- On this workstation, if Moon prepends the global Proto Go over mise's pinned Go and reports a compiler/GOROOT version mismatch, run `MOON_TOOLCHAIN_FORCE_GLOBALS=true mise exec -- moon ...`; do not change repository pins for that local collision.
